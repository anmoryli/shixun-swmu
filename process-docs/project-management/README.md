# 项目管理与功能点清单

## 统计口径

- 业务功能点：13 个（认证权限、仪表盘、城市、公司、销售地点、医生、药品、材料、两类政策、上传、前端入口/联调）。
- 后端接口端点：43 个映射（登录 JSON/表单按两个调用契约统计）。
- 计划周期：2026-07-13 至 2026-07-17，共 5 个工作日。
- 团队：成员1负责认证与后端基础，成员2负责仪表盘和基础资料，成员3负责医生/药品，成员4负责材料/政策/上传，成员5负责前端、联调、测试交付。

## 工作项字段

完整字段已写入 `feature-backlog.csv`：story、状态、处理人、模块、迭代、预计开始日期、预计结束日期、优先级顺序、优先级、重要程度、抄送人、父工作项、领域、预计工时、具体内容。

## 录入华为云 ProjectMan

按 CSV 每行创建 Story；`成员1` 至 `成员5` 替换为 ProjectMan 中的真实账号，迭代统一选择“迭代1”。状态初始为“待开发”，每天按实际进度改为进行中/已完成/阻塞。
