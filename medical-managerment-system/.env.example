NODE_ENV=production
# 同域部署推荐保持 /api；前后端分离时可填写完整网关地址。
VUE_APP_API_BASE_URL=/api
VUE_APP_API_TIMEOUT=10000
VUE_APP_WITH_CREDENTIALS=true

# 仅供本地 vue-cli-service serve 使用。
VUE_APP_PROXY_TARGET=http://localhost:8082
